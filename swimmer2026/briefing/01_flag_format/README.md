# 01_flag_format (10pt / 672 solves)

## 問題文

CTFでは、「答え」をFlag（フラグ）と呼びます。これから先、すべての調査事項はFlagとして提出する必要がありますから、まずは答え方を練習してみましょう。

さて、添付ファイル（`briefing_flag_format.jpg`）に写っているタワーの名前を、**英語表記** で解答してください。  
Flag形式は `SWIMMER{xxxx Tower}` です。もし `京都タワー` が答えである場合、Flagは `SWIMMER{Kyoto Tower}` となります。  
なお、各調査事項ごとにこのような「Flag形式」という例示を行いますので、提出時には必ず確認してください。

In CTFs, the "answer" is called a flag. From now on, every investigation item must be submitted as a flag, so let's practice how to answer.  

Answer the name of the tower shown in the attached file (`briefing_flag_format.jpg`) in **English**.  
The flag format is `SWIMMER{xxxx Tower}`. If the answer were `Kyoto Tower`, the flag should be `SWIMMER{Kyoto Tower}`.  
Each investigation item will include an example "flag format" like this, so be sure to check it when submitting.


## 配布ファイル

- [briefing_flag_format.jpg](./public/briefing_flag_format.jpg)

## 解法

添付されている画像は東京タワーですから、以下の通りです。  
（もし東京タワーに馴染みがない海外在住プレイヤーでも、Google画像検索で判別できます。）

Flag: **`SWIMMER{Tokyo Tower}`**